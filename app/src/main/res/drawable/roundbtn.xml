<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle" android:padding="@dimen/btn_radius">

    <solid android:color="@color/accent"/> <!-- this one is ths color of the Rounded Button -->
    <corners
        android:bottomRightRadius="@dimen/btn_radius"
        android:bottomLeftRadius="@dimen/btn_radius"
        android:topLeftRadius="@dimen/btn_radius"
        android:topRightRadius="@dimen/btn_radius"/>

</shape>